{% extends 'base.twig' %}

{% block title %}{{ page.title }} - {{ app.name }}{% endblock %}

{% block content %}
<div class="container">
	<div class="starter-template">
		<h1>{{ page.title }}</h1>
		<p class="lead">
			{{ page.content }}
		</p>
	</div>
</div>
{% endblock %}
